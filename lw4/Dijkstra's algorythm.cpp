#include <iostream>
#include <string.h>
#include "../lw3/Stack.h"

int main()
{
    Stack *stack = new Stack;
    stack == NULL;
    string s;

    while (0 == 0)
    {
        if (!EOF) cin >> s;
        else break;
        
        switch(s)
        {
            case "sin:"
            {
                break;
            }

            default:
                break;
        }
    }
}